{
  "name": "code_learning",
  "install": "python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements.txt",
  "terminals": [
    {
      "name": "web",
      "command": ".venv/bin/flask --app app run --host 0.0.0.0 --port 5000",
      "description": "Flask dev server for the Learning Notes app (http://localhost:5000)."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 5000
    }
  ]
}
